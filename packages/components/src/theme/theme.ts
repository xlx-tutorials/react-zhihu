const { PALETTE } = require('./palette')

const colors = {
  primary: PALETTE.primary,
  secondary: PALETTE.gray,
  text: PALETTE.black.base,
  background: '#f6f6f6',
  black: PALETTE.black,
  white: PALETTE.white,
}

export const THEME = {
  colors,
}
